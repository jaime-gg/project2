const router = require('express').Router();
const { Post, User, Comment } = require('../models');

// ADD ROUTES TO VIEW ALL THE USER'S STORIES

// ALLOW USERS TO EDIT THEIR OWN STORIES 

module.exports = router;